apiVersion: v1
kind: Secret
metadata:
  name: app2gcal-secret
type: Opaque
stringData:
  # Replace with actual Google service account credentials JSON
  # kubectl create secret generic app2gcal-secret --from-literal=google_credentials_json='{"type":"service_account",...}'
  google_credentials_json: "REPLACE_WITH_ACTUAL_CREDENTIALS"
